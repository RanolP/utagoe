{
  "name": "utagoe",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"tauri dev\" --kill-others",
    "build": "tsc && vite build && tauri build"
  },
  "dependencies": {
    "goober": "^2.0.37",
    "preact": "^10.5.13",
    "unistore": "^3.5.2",
    "wouter": "^2.7.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "concurrently": "^6.1.0",
    "prettier": "^2.3.0",
    "typescript": "^4.1.3",
    "vite": "^2.3.0"
  }
}
