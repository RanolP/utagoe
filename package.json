{
  "name": "utagoe",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"tauri dev\" --kill-others",
    "build": "tsc && vite build && tauri build",
    "serve": "vite preview"
  },
  "dependencies": {
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "concurrently": "^6.1.0",
    "typescript": "^4.1.3",
    "vite": "^2.3.0"
  }
}
